# Docs

| File | Purpose |
| --- | --- |
| `studynest_requirements_v2_1.pdf` | The requirements specification. Single source of truth for what StudyNest must do. |

When the spec is revised, increment its version (per spec §19) and add a new dated PDF rather than overwriting.
