# Docs

| File | Purpose |
| --- | --- | | The requirements specification. Single source of truth for what StudyNest must do. |

When the spec is revised, increment its version (per spec §19) and add a new dated PDF rather than overwriting.
